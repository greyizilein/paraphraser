import test from"node:test";import assert from"node:assert/strict";import{analyseNaturalness}from"../lib/naturalness.ts";
test("penalises repeated canned structure",()=>{const robotic="Furthermore, the system improves performance. Furthermore, the system improves reliability. Furthermore, the system improves consistency. Furthermore, the system improves efficiency.";const varied="The system improves performance, but that is only part of its value. Reliability matters too. Under heavier use, consistency becomes more important, while efficiency determines whether those gains remain practical.";assert.ok(analyseNaturalness(varied).score>analyseNaturalness(robotic).score);});
test("reports source structural overlap",()=>{const source="The project improves service quality by reducing delays and clarifying responsibility across the team.";const close="The project improves service quality by reducing delays and clarifying responsibility across the organisation.";const fresh="Clearer ownership and shorter delays allow the team to deliver a better service.";assert.ok(analyseNaturalness(close,source).sourceTrigramOverlap>analyseNaturalness(fresh,source).sourceTrigramOverlap);});
